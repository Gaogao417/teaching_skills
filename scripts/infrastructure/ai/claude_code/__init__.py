"""Claude Code shared infrastructure (architecture §3.2).

- :mod:`.client` — Claude Agent SDK ``query()`` boundary as an injectable port.
- :mod:`.pydantic_model` — PydanticAI ``Model`` bridge backed by the port.
"""
