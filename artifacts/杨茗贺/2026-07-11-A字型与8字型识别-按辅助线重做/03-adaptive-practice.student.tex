\documentclass{exam-zh}
\usepackage{edu-practice}

\examsetup{
  question/show-answer = false,
  fillin/show-answer = false,
  solution/show-solution = hide,
}

% ---------- 教师版解答样式 ----------
\newtcolorbox{solutionblock}{
  enhanced, breakable,
  colback=edu-blue-bg,
  colframe=edu-blue!25,
  boxrule=0pt,
  borderline west={2pt}{0pt}{edu-blue!50},
  arc=0pt,
  left=3mm, right=3mm, top=2mm, bottom=2mm,
  before skip=2mm,
  after skip=3mm,
  fontupper=\small,
}

% 解答步骤编号
\newcounter{solstep}
\newcommand{\solstep}[2]{%
  \stepcounter{solstep}%
  \par\medskip
  \noindent\hangindent=6mm
  {\color{edu-blue}\bfseries\textcircled{\scriptsize\thesolstep}\enspace #1}\enspace #2\par
}

\begin{document}

\section*{从完整辅助线图中抽取相似模型}

\section*{一、从完整图形中抽取相似模型}

\needspace{8\baselineskip}

\begin{problem}[points=10]
  \noindent
  \begin{minipage}[t]{\dimexpr\linewidth-60mm-6mm\relax}
    \vspace{0pt}
    观察图中的平行标记。把图中的实际对象代入模型 $l_1\parallel l_2$、模型点 $A$、两条截线 $AC,AD$：

（1）$l_1\parallel l_2$ 对应图中的 \underline{\hspace{2cm}}；
（2）模型点 $A$ 对应图中的 \underline{\hspace{1cm}}；
（3）$AC,AD$ 对应图中的两条直线 \underline{\hspace{1.8cm}}、\underline{\hspace{1.8cm}}；
（4）写出相似三角形 \underline{\hspace{4cm}}；
（5）判断该模型是 \underline{\hspace{1.5cm}} 型。


  \end{minipage}\hfill
  \begin{diagramcoltikz}{60mm}{第 1 题图}
  \input{\detokenize{build/diagram/jobs/identify-student-q1-prompt/rendered/prompt.fragment.tex}}
  \end{diagramcoltikz}
  \par\medskip
  \answerarea[34mm]
\end{problem}


\needspace{8\baselineskip}

\begin{problem}[points=10]
  \noindent
  \begin{minipage}[t]{\dimexpr\linewidth-60mm-6mm\relax}
    \vspace{0pt}
    观察图中的平行标记。把图中的实际对象代入模型 $l_1\parallel l_2$、模型点 $A$、两条截线 $AC,AD$：

（1）$l_1\parallel l_2$ 对应图中的 \underline{\hspace{2cm}}；
（2）模型点 $A$ 对应图中的 \underline{\hspace{1cm}}；
（3）$AC,AD$ 对应图中的两条直线 \underline{\hspace{1.8cm}}、\underline{\hspace{1.8cm}}；
（4）写出相似三角形 \underline{\hspace{4cm}}；
（5）判断该模型是 \underline{\hspace{1.5cm}} 型。


  \end{minipage}\hfill
  \begin{diagramcoltikz}{60mm}{第 2 题图}
  \input{\detokenize{build/diagram/jobs/identify-student-q2-prompt/rendered/prompt.fragment.tex}}
  \end{diagramcoltikz}
  \par\medskip
  \answerarea[34mm]
\end{problem}


\needspace{8\baselineskip}

\begin{problem}[points=10]
  \noindent
  \begin{minipage}[t]{\dimexpr\linewidth-60mm-6mm\relax}
    \vspace{0pt}
    观察图中的平行标记。把图中的实际对象代入模型 $l_1\parallel l_2$、模型点 $A$、两条截线 $AC,AD$：

（1）$l_1\parallel l_2$ 对应图中的 \underline{\hspace{2cm}}；
（2）模型点 $A$ 对应图中的 \underline{\hspace{1cm}}；
（3）$AC,AD$ 对应图中的两条直线 \underline{\hspace{1.8cm}}、\underline{\hspace{1.8cm}}；
（4）写出相似三角形 \underline{\hspace{4cm}}；
（5）判断该模型是 \underline{\hspace{1.5cm}} 型。


  \end{minipage}\hfill
  \begin{diagramcoltikz}{60mm}{第 3 题图}
  \input{\detokenize{build/diagram/jobs/identify-student-q3-prompt/rendered/prompt.fragment.tex}}
  \end{diagramcoltikz}
  \par\medskip
  \answerarea[34mm]
\end{problem}


\needspace{8\baselineskip}

\begin{problem}[points=10]
  \noindent
  \begin{minipage}[t]{\dimexpr\linewidth-60mm-6mm\relax}
    \vspace{0pt}
    观察图中的平行标记。把图中的实际对象代入模型 $l_1\parallel l_2$、模型点 $A$、两条截线 $AC,AD$：

（1）$l_1\parallel l_2$ 对应图中的 \underline{\hspace{2cm}}；
（2）模型点 $A$ 对应图中的 \underline{\hspace{1cm}}；
（3）$AC,AD$ 对应图中的两条直线 \underline{\hspace{1.8cm}}、\underline{\hspace{1.8cm}}；
（4）写出相似三角形 \underline{\hspace{4cm}}；
（5）判断该模型是 \underline{\hspace{1.5cm}} 型。


  \end{minipage}\hfill
  \begin{diagramcoltikz}{60mm}{第 4 题图}
  \input{\detokenize{build/diagram/jobs/identify-student-q4-prompt/rendered/prompt.fragment.tex}}
  \end{diagramcoltikz}
  \par\medskip
  \answerarea[34mm]
\end{problem}



\end{document}
