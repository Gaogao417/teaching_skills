# Contract Audit

Verdict: **APPROVE_WITH_NOTES**

The two dedicated auditor attempts failed before returning a verdict because their response stream disconnected. A conservative main-session audit checked both contracts against the slice plan, schema reference, current number-review service, and representative resolved assignments.

Required notes copied downstream:

1. Treat duplicate bank ids and malformed manifests as non-selectable discovery errors; never choose one ambiguously.
2. Resolve prompt diagrams from the student question block and solution diagrams from the teacher answer-space/question solution data without invoking renderers.
3. Render all authored strings with DOM text nodes; static trusted UI markup may use templates, but no assignment-derived value may enter `innerHTML`.
4. Map requested ids to discovered records before filesystem access and reject escaped or symlinked assets.
5. Keep reciprocal URLs explicitly configurable with defaults 8876/8877.
