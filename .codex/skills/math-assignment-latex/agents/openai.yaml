interface:
  display_name: "Assignment LaTeX 渲染"
  short_description: "将定稿 assignment YAML 直接渲染并检查为 TeX。"
  default_prompt: "请使用 $math-assignment-latex 验证这份已定稿的 assignment.yaml，直接渲染、检查并用 VS Code 打开 TeX；仅在明确要求时编译 PDF。"
