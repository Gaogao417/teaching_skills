# 结构分析：一次函数增减性分类讨论

## 原题
已知一次函数 $y=kx+b$ 的自变量的取值范围是 $-3$ 到 $6$，相应的函数值的取值范围是 $-5$ 到 $-2$，求这个一次函数的解析式。

## 一、题目场景
- 数学对象：一次函数 $y=kx+b$
- 变量/参数：自变量 $x$，函数值 $y$，斜率 $k$，截距 $b$
- 函数/图形：一条直线
- 已知条件：
  - $x$ 的取值范围：$-3 \leq x \leq 6$
  - $y$ 的取值范围：$-5 \leq y \leq -2$
- 要求目标：求 $k$ 和 $b$ 的值，即确定解析式

## 二、核心结构
- 表面考点：求一次函数解析式（待定系数法）
- 本质考点：利用一次函数的增减性进行分类讨论——斜率 $k$ 的正负决定端点对应关系
- 一句话问题模式：给定 $x$ 范围和 $y$ 范围，求解析式，需分类讨论 $k>0$ 与 $k<0$ 两种情况

## 三、关键转化
- 最关键的转化：将"取值范围"转化为"端点对应"——当 $k>0$ 时 $x_{\min}$ 对应 $y_{\min}$、$x_{\max}$ 对应 $y_{\max}$；当 $k<0$ 时反过来
- 为什么降低计算量：直接建立方程组求解，避免画图或列表试算
- 不转化时的低效路径：画图猜测、代入多组数值试算、忽略 $k<0$ 的情况导致漏解

## 四、标准路径骨架
1. 先做什么：识别需要分类讨论——$k$ 的正负未知，所以分 $k>0$ 和 $k<0$ 两种情况
2. 再做什么：$k>0$（递增）时：$x=-3 \Rightarrow y=-5$，$x=6 \Rightarrow y=-2$，代入得方程组
3. 建立什么关系：$\begin{cases} -3k + b = -5 \\ 6k + b = -2 \end{cases}$，解得 $k=\frac{1}{3}$，$b=-4$，即 $y=\frac{1}{3}x-4$
4. 如何求解：$k<0$（递减）时：$x=-3 \Rightarrow y=-2$，$x=6 \Rightarrow y=-5$，代入得方程组 $\begin{cases} -3k + b = -2 \\ 6k + b = -5 \end{cases}$，解得 $k=-\frac{1}{3}$，$b=-3$，即 $y=-\frac{1}{3}x-3$
5. 需要检查什么：验证两解均满足 $y$ 范围为 $[-5, -2]$；确认没有遗漏（$k=0$ 不可能，因为 $y$ 范围非零）

## 五、出题人逻辑
- 诱导学生硬算的位置：学生容易只考虑 $k>0$（递增）的情况，直接把最小的 $x$ 配最小的 $y$
- 真正的捷径：先判断 $k$ 的符号有几种可能，再分别建立方程组
- 训练的可迁移能力：分类讨论思想——当关键参数的正负影响结论时，必须分情况处理

## 六、学生卡点预测
- 基础薄弱学生：不知道为什么要分类讨论，容易漏掉 $k<0$ 的情况，只得到一个解
- 中等学生：知道要讨论但可能混淆端点对应关系（$k<0$ 时谁配谁搞反）
- 较强学生：能顺利分类求解，但可能忘记验证结果是否满足原题条件

## 七、变式原则
- 必须保留：$x$ 范围、$y$ 范围、求解析式的结构
- 可以变化：$x$ 和 $y$ 的具体范围数值；改为已知图像过两个点的形式；增加 $k>0$ 或 $k<0$ 的限制条件使问题变简
- 容易跑偏：变成求值域的逆问题后学生可能不理解题意；数值过大导致计算出错

## 八、交付给下一阶段的结构摘要
```json
{
  "problem_pattern": "给定自变量范围和函数值范围，求一次函数解析式（需分类讨论k的符号）",
  "core_transformation": "将取值范围转化为端点对应关系：k>0时同向配对，k<0时反向配对，分别建立二元一次方程组",
  "solution_skeleton": [
    "识别k的符号未知，分k>0和k<0两种情况",
    "k>0时：x_min配y_min，x_max配y_max，建立方程组求解",
    "k<0时：x_min配y_max，x_max配y_min，建立方程组求解",
    "验证两解均满足原题条件"
  ],
  "common_blockers": {
    "low": ["不知道为什么要分类讨论", "只考虑k>0漏掉一个解"],
    "middle": ["k<0时端点对应关系搞反", "解方程组计算出错"],
    "strong": ["忘记验证结果"]
  },
  "variation_rules": {
    "keep": ["自变量范围+函数值范围的结构", "需要判断k的符号", "待定系数法求解"],
    "change_allowed": ["具体数值范围", "增加k的符号限制条件", "改为过两点求解析式"],
    "avoid": ["数值过大导致计算负担", "改为二次或其他函数（超出一次函数范畴）"]
  }
}
```

下一步建议：使用 math-student-explanation-html，输入本结构分析 + 学生画像 + 本次目标。
